function printInput(input, numberOfTimes) {
    for (let i = 0; i < numberOfTimes; i++) {
      console.log(`Inputted: ${input}`);
    }
  }
  
  printInput(2, 3);
  // prints out the number 2 (first argument) three times (second argument)
  // Inputted: 2
  // Inputted: 2
  // Inputted: 2
  
  printInput("Ironhack is the best!", 2);
  
  // Inputted: Ironhack is the best!
  // Inputted: Ironhack is the best!
  