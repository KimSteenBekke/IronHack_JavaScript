const fact = "I love web dev!";
console.log(`"${fact}" is a string and it's length is ${fact.length}.`);

// "I love web dev!" is a string, and it's length is 15.

const fact2 = `Ironhack sucks and that is a fact!`;
console.log(`"${fact2}" is a string and it's length is ${fact2.length} but it still sucks!"`);